import React from "react";
import ReactDOM from "react-dom/client";
import SchoolMS from "./components/SchoolMS";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SchoolMS />
  </React.StrictMode>
);
